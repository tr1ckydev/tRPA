import { exportToClient, legacy } from "../../src/server";
import express from "express";

const app = express();

app.get("/", (req, res) => {
    res.header(field);
    res.sen;
});

app.listen(3000);